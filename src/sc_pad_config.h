#pragma once

// Set to 1 in platformio.ini to retain the SYSTEM-page 180-degree display
// rotation option. Keeping it at 0 uses the native RGB output path instead.
#ifndef SC_PAD_ENABLE_DYNAMIC_ROTATION
#define SC_PAD_ENABLE_DYNAMIC_ROTATION 0
#endif
